# AGENTS.md — 项目约束

本文件对在本仓库工作的 AI agent 与贡献者生效。

## 文档存放约束（强制）

- **本仓库生成的所有 Markdown 文档（学习路线、专项文档等）必须存放在以 `_start` 结尾的文件夹中**，命名格式为 `<主题>_start/`，例如 `typescript_start/`、`golang_start/`、`vim_start/`。
- 新建主题路线时，先创建对应的 `<主题>_start/` 目录，再把 `.md` 文件写入其中；**禁止把 `.md` 文档直接生成在仓库根目录**。
- 例外（不受本约束限制）：
  - `README.md`（仓库总索引）
  - `.pi/` 目录下的技能定义与参考文件

## 文档同步约束

- 新增、移动或删除文档后，必须同步更新 `README.md` 的「路线索引」表格与「目录结构」小节，保持索引与事实一致。
- 文档大改后回头同步其头部声明（总时长、版本基线、内容构成）。
